# Boot directly into LatterOS after a short delay.
timeout: 3

# Start the boot menu and LatterOS desktop at a stable guest resolution.
interface_resolution: 1280x800

/LatterOS
    protocol: limine
    path: boot():/boot/kernel
    resolution: 1280x800x32
